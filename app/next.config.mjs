/** @type {import('next').NextConfig} */
const nextConfig = {
  // Allow importing dashboard.json from shared data/ dir
  experimental: {
    // Enable if using Turbopack
  },
};

export default nextConfig;
